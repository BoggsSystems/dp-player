export class Redemption {
  // tslint:disable-next-line:variable-name
  _id: string;
  points: number;
  reward: string;
  createdBy: string;
  updatedAt: Date;
  createdAt: Date;
  userId: string; /* Only used for requests */
}
